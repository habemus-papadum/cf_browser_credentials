{
  "name": "cf_browser_credentials",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "cf_browser_credentials — a lockstep-versioned TypeScript monorepo.",
  "author": "Nehal Patel <nehal@alum.mit.edu>",
  "license": "MIT",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "format": "biome check --write .",
    "new-package": "node scripts/new-package.mjs",
    "version:check": "node scripts/versioning.mjs current",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitepress": "^1.6.4",
    "vitest": "^3.2.0"
  },
  "packageManager": "pnpm@11.9.0"
}
