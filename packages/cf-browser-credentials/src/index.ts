/**
 * A TypeScript library for minting ephemeral credentials from a Cloudflare worker for an otherwise static site
 *
 * @packageDocumentation
 */

/** The published package name — handy for smoke tests. */
export const name = "@habemus-papadum/cf-browser-credentials";

/** Greet someone. Replace with your library's real API. */
export function greet(who: string): string {
  return `Hello, ${who}!`;
}
